rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles temporaires très permissives pour résoudre le problème
    match /{document=**} {
      allow read, write: if true;
    }
  }
}