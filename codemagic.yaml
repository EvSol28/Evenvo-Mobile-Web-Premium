workflows:
  ios-workflow:
    name: WORKFLOW_IOS
    max_build_duration: 60
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - name: Installer les dépendances Flutter
        script: |
          flutter pub get
      - name: Installer les pods iOS
        script: |
          cd ios
          pod install
